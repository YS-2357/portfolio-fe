---
태그: []
이름: "5일차-250516(금)"
---

# 5일차-250516(금)


## 🗓️ 일자: 250516(금)


## ✍ 작성자: 정영선


---


## ✅ 오늘 할 일


- [x] 문제 정의 및 폴더 구조 정리하기

- [x] `loader_main()` 구조 점검 및 모듈화 정비

- [x] `data_loader.py`에서 `base_dir` 자동 설정 및 경로 오류 수정

---


## 🎯 오늘 한 일


1. **RAG 처리 흐름 정의 및 세부 설계:**

1. **폴더 구조 및 경로 기준 정비:**

1. **LangSmith 연동 정보 공유:**

1. **loader_main 구조 점검 및 통합 실행 가능 구조로 리팩토링:**

1. `path.py` 생성

---


## 🛠️ Issues & Challenges


- **문제 1:** `data_loader` 내 경로 설정 문제로 `FileNotFoundError` 발생

- **문제 2:** Jupyter 환경에서 `.py` 수정사항이 즉시 반영되지 않음

---


## 💡 Notes & Ideas


- **VectorDB 하이퍼파라미터 조정:**

- **retrieval 성능 향상을 위한 질의 재구성 고려:**

- **텍스트 전처리:**

---


## 🔄 내일 할 일


- [ ] 전체 실행 흐름 정리용 `main.py` 또는 각 `_main.py` 파일 구성하기

- [ ] vectorDB 메타데이터 살펴보고, 적절한 문서 탐색 기준 수립

- [ ] streamlit을 활용한 최소 챗봇형 결과 창 시도

---


## 📝 Reflection


- 다양한 모듈이 연결되는 RAG 구조에서 **경로 관리 및 config 구조의 일관성 유지**가 핵심이라는 점을 다시 실감함.

- 실험 설계와 설정값(하이퍼파라미터)의 명문화가 향후 실험 비교 및 재현에 매우 중요함.
langsmith를 이용해 로깅을 해야함

---


## 🖥️ 코드 스니펫


```python
def get_project_root_dir(project_name: str = "2025-LLM-Project") -> str:
    """
    현재 파일 또는 실행 위치에서 프로젝트 루트 디렉토리를 탐색합니다.
    
    Args:
        project_name (str): 루트 디렉토리명
    
    Returns:
        str: 루트 디렉토리의 절대 경로
    """
    cwd = os.path.abspath(os.getcwd())
    while cwd != os.path.dirname(cwd):  # 루트까지 올라감
        if os.path.basename(cwd) == project_name:
            return cwd
        cwd = os.path.dirname(cwd)
    raise RuntimeError(f"❌ 루트 디렉토리({project_name})를 찾을 수 없습니다.")
```

